# ==============================
# API 环境配置（开发）
# ==============================

# 接口模式：real=请求后端真实接口，mock=使用本地 mock 数据
VITE_API_MODE=real

# API 基础地址（不带末尾斜杠）
VITE_API_BASE_URL=http://127.0.0.1:8080

# mock 模式下接口延迟（毫秒），仅当 mode=mock 时生效
VITE_MOCK_DELAY_MS=180
